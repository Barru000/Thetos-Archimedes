# Minimal static scaffold

Run locally:
- Install Live Server extension in VS Code and click "Go Live", or
- From the project root run: `python -m http.server 8000` and open http://localhost:8000

Deploy:
- Push to a Git repo and connect to a static host (Vercel, Netlify, Cloudflare Pages) or upload to S3 + CloudFront.